#include <iostream>
#include <vector>
#include <map>
#include<bits/stdc++.h>
using namespace std;

//Driver
int main()
{
    // Iterating over whole array
    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    for (auto i : v)
        cout << i << " ";

    cout << '\n';

    return 0;

}
